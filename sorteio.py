import random #biblioteca que gera numeros aleatorios

num = random.randint(1, 10) #randint é uma função em que faz gerar numeros inteiros, já que a maquina so conta de 0 a 1.
print(num) #aqui o print pode ser diretamente o valor